apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sidecar-lab

resources:
  - namespace.yaml
  - configmap.yaml
  - job.yaml
  - mock-backend.yaml
  - networkpolicy.yaml

commonLabels:
  app.kubernetes.io/part-of: sidecar-lab
  app.kubernetes.io/managed-by: kustomize
